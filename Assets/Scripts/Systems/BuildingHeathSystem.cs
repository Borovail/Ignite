using System;
using Unity.Entities;

namespace Assets.Scripts
{
    public partial struct BuildingHeathSystem : ISystem
    {
        public void OnUpdate(ref SystemState state)
        {
            //foreach (var VARIABLE in SystemAPI.Query<>())
            //{
            //}
        }
    }
}